@model SV22T1020761.Models.Catalog.ProductDetailsViewModel
@{
    ViewBag.Title = "Chỉnh sửa Sản phẩm";
}
<h2>Chỉnh sửa Sản phẩm</h2>
<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Product.ProductID" />
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label for="Product.ProductName">Tên sản phẩm *</label>
                <input asp-for="Product.ProductName" class="form-control" />
                <span asp-validation-for="Product.ProductName" class="text-danger"></span>
            </div>
        </div>
        <div class="col-md-3">
            <div class="form-group">
                <label for="Product.Price">Giá *</label>
                <input asp-for="Product.Price" class="form-control" />
                <span asp-validation-for="Product.Price" class="text-danger"></span>
            </div>
        </div>
        <div class="col-md-3">
            <div class="form-group">
                <label for="Product.Photo">Ảnh chính</label>
                <div class="input-group">
                    <select name="Product.Photo" class="form-control">
                        <option value="">-- Chọn từ thư viện --</option>
                        @{
                            var photos = ViewBag.Photos as List<SV22T1020761.Models.Catalog.ProductPhoto>;
                            if (photos != null && photos.Count > 0)
                            {
                                foreach (var photo in photos)
                                {
                                    var isSelected = Model?.Product?.Photo == photo.Photo;
                                    <option value="@photo.Photo" @(isSelected ? "selected" : "")>@photo.Photo</option>
                                }
                            }
                        }
                    </select>
                    <a asp-action="CreatePhoto" asp-route-id="@Model.Product.ProductID" class="btn btn-sm btn-info" title="Thêm ảnh mới">
                        <i class="bi bi-plus-circle"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label for="Product.ProductDescription">Mô tả sản phẩm</label>
                <textarea asp-for="Product.ProductDescription" class="form-control" rows="3"></textarea>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group">
                <label for="Product.Unit">Đơn vị tính</label>
                <input asp-for="Product.Unit" class="form-control" placeholder="Ví dụ: cái, bộ, hộp..." />
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
                <label for="Product.SupplierID">Nhà cung cấp</label>
                <select asp-for="Product.SupplierID" class="form-control">
                    <option value="0">-- Chọn nhà cung cấp --</option>
                    @foreach (var supplier in ViewBag.Suppliers ?? new List<dynamic>())
                    {
                        <option value="@supplier.SupplierID">@supplier.SupplierName</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                <label for="Product.CategoryID">Loại hàng</label>
                <select asp-for="Product.CategoryID" class="form-control">
                    <option value="0">-- Chọn loại hàng --</option>
                    @foreach (var category in ViewBag.Categories ?? new List<dynamic>())
                    {
                        <option value="@category.CategoryID">@category.CategoryName</option>
                    }
                </select>
            </div>
        </div>
        <div class="col-md-4">
            <div class="form-group">
                <label for="Product.Quantity">Số lượng tồn</label>
                <input asp-for="Product.Quantity" class="form-control" />
            </div>
        </div>
    </div>
    <div class="form-group">
        <div class="form-check">
            <input type="checkbox" asp-for="Product.IsSelling" class="form-check-input" />
            <label class="form-check-label" for="Product.IsSelling">Đang bán</label>
        </div>
    </div>
    <button type="submit" class="btn btn-primary">Lưu</button>
    <a asp-action="Index" class="btn btn-secondary">Hủy</a>
</form>

@{
    //if (Model.ProductId > 0) {
    await Html.RenderPartialAsync("ListPhotos", Model);
    await Html.RenderPartialAsync("ListAttributes", Model);
    // }
}