@model SV22T1020761.Models.Common.PagedResult<SV22T1020761.Models.Supplier>
@{
    ViewBag.Title = "Danh sách Nhà cung cấp";
}
@section PageToolBar {
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i>
        Thêm mới
    </a>
}
<div class="card mb-3">
    <div class="card-body">
        <form id="formSearch" method="get" action="~/Supplier/Index">
            <div class="input-group">
                <input type="text" name="searchValue" value="@Context.Request.Query["searchValue"]" class="form-control" placeholder="Tên nhà cung cấp..." autofocus />
                <button class="btn btn-info d-flex align-items-center">
                    <i class="bi bi-search me-1"></i>
                </button>
            </div>
        </form>
    </div>
</div>
<div class="mb-2 text-muted">
    Danh sách này có <strong>@Model?.RowCount ?? 0</strong> nhà cung cấp
</div>
<div class="card">
    <div class="card-body p-1">
        <table class="table table-bordered table-hover mb-0">
            <thead class="table-secondary">
                <tr>
                    <th>Tên nhà cung cấp</th>
                    <th>Liên hệ</th>
                    <th>Địa chỉ</th>
                    <th class="text-center" style="width:100px;">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @if (Model?.DataItems != null && Model.DataItems.Count > 0)
                {
                    foreach (var item in Model.DataItems)
                    {
                        <tr>
                            <td>@item.SupplierName</td>
                            <td>@item.ContactName</td>
                            <td>@item.Address</td>
                            <td class="text-center text-nowrap">
                                <a asp-action="Edit" asp-route-id="@item.SupplierID" class="btn btn-sm btn-primary">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.SupplierID" class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center">Không có dữ liệu</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<nav aria-label="Page navigation">
    <ul class="pagination justify-content-center mt-3">
        @for (int i = 1; i <= Model?.PageCount; i++)
        {
            <li class="page-item @(i == Model.Page ? "active" : "")">
                <a class="page-link" href="?page=@i&searchValue=@Context.Request.Query["searchValue"]">@i</a>
            </li>
        }
    </ul>
</nav>

<script>
    $(document).ready(function () {
        $("#searchForm").on("submit", function (e) {
            e.preventDefault();

            $.ajax({
                url: "@Url.Action("Search", "Supplier")",
                type: "POST",
                data: $(this).serialize(),
                success: function (result) {
                    $("#supplierTableContainer").html(result);
                },
                error: function () {
                    alert("An error occurred while processing your request.");
                }
            });
        });
    });
</script>

<div>
    <form id="searchForm">
        <input type="text" name="SearchValue" placeholder="Search..." />
        <button type="submit">Search</button>
    </form>
</div>

<div id="supplierTableContainer">
    @Html.Partial("_SupplierTable", Model)
</div>



