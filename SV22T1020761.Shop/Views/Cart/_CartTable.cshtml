@model System.Collections.Generic.List<SV22T1020761.Shop.AppCodes.CartItem>
@using System.Linq
<table class="table">
    <thead>
        <tr>
            <th>S?n ph?m</th>
            <th>S? lý?ng</th>
            <th>Giá</th>
            <th>Thành ti?n</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.ProductName</td>
                <td>
                    <form class="d-inline" method="post" asp-controller="Cart" asp-action="Update">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="productId" value="@item.ProductID" />
                        <input type="number" name="qty" value="@item.Qty" min="1" style="width:80px;" />
                        <button class="btn btn-sm btn-primary">C?p nh?t</button>
                    </form>
                </td>
                <td>@item.Price</td>
                <td>@(item.Price * item.Qty)</td>
                <td>
                    <form method="post" asp-controller="Cart" asp-action="Remove">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="productId" value="@item.ProductID" />
                        <button class="btn btn-sm btn-danger">Xóa</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

@{
    var total = Model.Sum(i => i.Price * i.Qty);
}
<div class="d-flex justify-content-between align-items-center border-top pt-3">
    <div>
        <div class="h5 mb-0">T?ng: <strong class="text-primary">@total.ToString("C")</strong></div>
        <div class="text-muted small">(Chýa bao g?m phí v?n chuy?n)</div>
    </div>
    <div>
        <a class="btn btn-success btn-lg" asp-controller="Checkout" asp-action="Index">Ð?t mua hàng</a>
    </div>
</div>
